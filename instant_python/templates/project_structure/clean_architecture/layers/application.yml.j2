{#
  Application Layer Template for Clean Architecture

  This layer typically contains use cases and application services.
  Currently no built-in features contribute to this layer.
#}
- name: application
  type: directory
  python: True