- name: test
  type: directory
  python: True
  children:
    - name: shared
      type: directory
      python: True
      children:
        - name: domain
          type: directory
          python: True
          children:
            - name: random_generator
              type: file
              extension: .py
        {% if "event_bus" in built_in_features %}
        - name: infra
          type: directory
          python: True
          children:
            - name: event_bus/mock_event_bus
              type: file
              extension: .py
        {% endif %}
    {% set context %}
    {% include "project_structure/domain_driven_design/bounded_context.yml.j2" %}
    {% endset %}
    {{ context | indent(4) }}